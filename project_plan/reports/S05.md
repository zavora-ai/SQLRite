# S05 Sprint Report

Sprint: S05 (2026-04-27 to 2026-05-10)  
Phase: B - SQL-Native Retrieval Core  
Status: Completed  
Date: February 28, 2026

## Scope IDs Completed

1. SD-06
2. SD-07
3. PB-D02
4. N90-02 (functions and retrieval index DDL subset)
5. GV-01

## Delivered Artifacts

1. Retrieval SQL function extensions in `src/main.rs`:
   - `embed(text)`
   - `bm25_score(query, document)`
   - `hybrid_score(vector_score, text_score, alpha)`
2. Retrieval index DDL parser/executor in `src/main.rs`:
   - `CREATE VECTOR INDEX ... USING HNSW [WITH (...)]`
   - `CREATE TEXT INDEX ... USING FTS5 [WITH (...)]`
   - `DROP VECTOR INDEX ...`
   - `DROP TEXT INDEX ...`
3. Metadata catalog migration in `src/lib.rs`:
   - schema version `3`
   - `retrieval_indexes` table
   - `retrieval_index_catalog` view
4. SQL CLI bootstrap hardening in `src/main.rs`:
   - SQL sessions now initialize schema migrations before statement execution.
5. SQL shell example catalog updates in `src/main.rs`:
   - `hybrid`, `vector_ddl`, `index_catalog`
6. README and changelog updates for retrieval function/DDL usage and outputs in `README.md` and `CHANGELOG.md`.

## Verification Evidence

Quality gates:

1. `cargo fmt --all`
2. `cargo clippy --all-targets --all-features -- -D warnings`
3. `cargo test`

SQL DDL/function smoke:

1. `project_plan/reports/s05_sql_smoke.log`
2. Covers init, retrieval SQL functions, vector/text index DDL, catalog query, and drop path.
3. Observed:
   - schema version `3` initialized
   - vector and text retrieval indexes created and listed in catalog
   - `embed`, `bm25_score`, and `hybrid_score` returned expected numeric output

Benchmark and eval evidence:

1. Benchmark command log: `project_plan/reports/s05_benchmark.log`
2. Benchmark JSON artifact: `project_plan/reports/s05_benchmark.json`
3. Eval command log: `project_plan/reports/s05_eval.log`
4. Eval JSON artifact: `project_plan/reports/s05_eval.json`

Selected benchmark observations (`project_plan/reports/s05_benchmark.json`):

1. `qps=996.33`
2. `top1_hit_rate=1.0`
3. `latency.p95_ms=1.7316`

## Sprint Gate Conclusion

1. SD-06 is complete for Sprint 5 scope: retrieval SQL function set (`embed`, `bm25_score`, `hybrid_score`) is implemented, documented, and tested.
2. SD-07 and PB-D02 are complete: retrieval index DDL is implemented with `USING HNSW` and `USING FTS5`, including catalog metadata and drop semantics.
3. N90-02 is progressed on schedule with operators/functions/DDL now available in SQL mode.
4. Sprint 5 quality and evidence gates are green with reproducible logs and JSON artifacts.
