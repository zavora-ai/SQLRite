# S01 Sprint Report

Sprint: S01 (2026-03-02 to 2026-03-15)
Phase: A - Productization and Distribution
Status: Completed
Date: February 28, 2026

## Scope IDs Completed

1. SD-01
2. SD-13
3. PA-D01
4. N90-01
5. GV-01

## Delivered Artifacts

1. Unified CLI entrypoint implemented in `/Users/jameskaranja/Developer/projects/SQLRight/src/main.rs`.
2. Cargo default run target set to `sqlrite` in `/Users/jameskaranja/Developer/projects/SQLRight/Cargo.toml`.
3. Command map RFC published at `/Users/jameskaranja/Developer/projects/SQLRight/docs/rfcs/0001-unified-cli-command-map.md`.
4. Runtime profile contract published at `/Users/jameskaranja/Developer/projects/SQLRight/docs/runtime_config_profiles.md`.
5. CI smoke checks for unified CLI added to `/Users/jameskaranja/Developer/projects/SQLRight/.github/workflows/ci.yml`.
6. Release bootstrap artifact pipeline added to `/Users/jameskaranja/Developer/projects/SQLRight/.github/workflows/release-bootstrap.yml`.

## Functional Coverage

Unified command surface now includes:

1. `init`
2. `sql`
3. `ingest`
4. `query`
5. `serve`
6. `backup`
7. `benchmark`
8. `doctor`

No-argument `cargo run` behavior remains backward-compatible with seeded demo flow.

## Verification Evidence

Quality gates run locally:

1. `cargo fmt --all`
2. `cargo clippy --all-targets --all-features -- -D warnings`
3. `cargo test`

CLI smoke evidence (local):

1. `cargo run --bin sqlrite -- --help`
2. `cargo run --bin sqlrite -- init --db sprint1_cli_demo.db --seed-demo --profile balanced --index-mode brute_force`
3. `cargo run --bin sqlrite -- query --db sprint1_cli_demo.db --text "local memory" --top-k 2`
4. `cargo run --bin sqlrite -- doctor --db sprint1_cli_demo.db`
5. `cargo run --bin sqlrite -- sql --db sprint1_cli_demo.db --execute "SELECT id, doc_id FROM chunks ORDER BY id LIMIT 2;"`
6. `cargo run --bin sqlrite -- ingest --db sprint1_cli_demo.db --id sprint1-extra --doc-id doc-extra --content "CLI ingest path" --embedding 0.2,0.8,0.0 --tenant sprint --source cli`
7. `cargo run --bin sqlrite -- backup --source sprint1_cli_demo.db --dest sprint1_cli_backup.db`
8. `cargo run --bin sqlrite -- benchmark --corpus 500 --queries 20 --warmup 5 --embedding-dim 32 --top-k 5 --candidate-limit 100 --batch-size 100 --alpha 0.65 --profile balanced --index-mode brute_force`

## Risks and Follow-ups

1. The umbrella CLI currently coexists with specialized binaries; parity migration should continue in S02/S03.
2. `sqlrite serve` is validated for startup contract; long-running operational soak remains a later sprint gate.
3. Packaging channels (Homebrew/winget/apt) are scoped to S02.
