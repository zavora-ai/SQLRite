# S06 Sprint Report

Sprint: S06 (2026-05-11 to 2026-05-24)  
Phase: B - SQL-Native Retrieval Core  
Status: Completed  
Date: February 28, 2026

## Scope IDs Completed

1. SD-06
2. SD-08
3. SQ-01
4. SQ-04
5. PB-D03
6. N90-02 (hybrid scoring and fallback subset)
7. GV-01

## Delivered Artifacts

1. Hybrid planner fallback hardening in `/Users/jameskaranja/Developer/projects/SQLRight/src/lib.rs`:
   - vector candidate planner now falls back to brute-force scoring from stored embeddings when index/ANN candidates are absent or unhealthy
   - fallback path honors query filters (`doc_id`, metadata filters)
2. Deterministic ranking behavior hardening in `/Users/jameskaranja/Developer/projects/SQLRight/src/lib.rs`:
   - repeated-run deterministic ordering tests added for equal-score cases
   - fallback behavior tests added for absent index and index-dimension mismatch scenarios
3. SQL hybrid one-statement usage evidence:
   - `hybrid_score`, `bm25_score`, and vector distance expression used in a single SQL statement
4. Documentation updates for fallback semantics in `/Users/jameskaranja/Developer/projects/SQLRight/README.md`.

## Verification Evidence

Quality gates:

1. `cargo fmt --all`
2. `cargo clippy --all-targets --all-features -- -D warnings`
3. `cargo test`

SQL fallback and deterministic smoke:

1. `/Users/jameskaranja/Developer/projects/SQLRight/project_plan/reports/s06_sql_smoke.log`
2. Observed fallback evidence:
   - `--index-mode disabled` with `--candidate-limit 1` still returns semantic best match (`best`) rather than recency-biased row.
3. Observed deterministic evidence:
   - repeated hybrid query runs (`run=1..3`) return identical ordering:
     - `a-chunk` then `b-chunk`
4. Observed SQL-only hybrid evidence:
   - one SQL statement computes vector score + lexical score + `hybrid_score`, sorted deterministically by `hybrid DESC, id ASC`.

Benchmark and eval evidence:

1. Benchmark log: `/Users/jameskaranja/Developer/projects/SQLRight/project_plan/reports/s06_benchmark.log`
2. Benchmark JSON: `/Users/jameskaranja/Developer/projects/SQLRight/project_plan/reports/s06_benchmark.json`
3. Eval log: `/Users/jameskaranja/Developer/projects/SQLRight/project_plan/reports/s06_eval.log`
4. Eval JSON: `/Users/jameskaranja/Developer/projects/SQLRight/project_plan/reports/s06_eval.json`

Selected benchmark observations (`s06_benchmark.json`):

1. `qps=474.39`
2. `top1_hit_rate=1.0`
3. `latency.p95_ms=2.2937`

## Sprint Gate Conclusion

1. SD-08 is complete for Sprint 6 scope: planner fallback now executes brute-force vector scoring when index/ANN paths cannot provide usable candidates.
2. PB-D03 and SQ-04 are complete for Sprint 6 scope: deterministic hybrid ordering is explicitly tested across repeated runs and tie conditions.
3. SQ-01 is complete for Sprint 6 scope: hybrid retrieval is demonstrably expressible and runnable in one SQL statement.
4. SD-06 remains complete and validated with the SQL hybrid function path (`vector`/distance + `bm25_score` + `hybrid_score`).
