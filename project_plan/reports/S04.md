# S04 Sprint Report

Sprint: S04 (2026-04-13 to 2026-04-26)  
Phase: B - SQL-Native Retrieval Core  
Status: Completed  
Date: February 28, 2026

## Scope IDs Completed

1. SD-05
2. PB-D01
3. N90-02 (operator/functions subset)
4. GV-01

## Delivered Artifacts

1. SQL operator syntax support in `sqlrite sql` for:
   - `<->` (L2 distance)
   - `<=>` (cosine distance)
   - `<#>` (negative inner product)
2. Vector SQL helper functions registered on SQL connections in `src/main.rs`:
   - `vector(text_or_blob)`
   - `l2_distance(lhs, rhs)`
   - `cosine_distance(lhs, rhs)`
   - `neg_inner_product(lhs, rhs)`
   - `vec_dims(vector_expr)`
   - `vec_to_json(vector_expr)`
3. SQL operator rewrite engine in `src/main.rs` that translates operator syntax to deterministic function calls while preserving quoted strings/comments.
4. Interactive shell retrieval examples updated with `vector` case in `src/main.rs`.
5. Dependency update in `Cargo.toml` to enable `rusqlite` `functions` feature.
6. README additions for SQL-native operator usage and sample output in `README.md`.

## Verification Evidence

Quality gates:

1. `cargo fmt --all`
2. `cargo clippy --all-targets --all-features -- -D warnings`
3. `cargo test`

Operator/unit regression suite:

1. `vector_operator_rewrite_maps_to_distance_functions`
2. `vector_operator_rewrite_ignores_literals_and_comments`
3. `retrieval_sql_functions_compute_expected_distances`

Runtime SQL smoke:

1. `cargo run -- init --db s04_demo.db --seed-demo --profile balanced --index-mode brute_force`
2. `cargo run -- sql --db s04_demo.db --execute "SELECT id, embedding <-> vector('0.95,0.05,0.0') AS l2, embedding <=> vector('0.95,0.05,0.0') AS cosine_distance, embedding <#> vector('0.95,0.05,0.0') AS neg_inner FROM chunks ORDER BY l2 ASC, id ASC LIMIT 3;"`
3. `printf '.example vector --run\n.exit\n' | cargo run -- sql --db s04_demo.db`

Observed smoke result:

1. Operator query returned ordered rows with numeric `l2`, `cosine_distance`, and `neg_inner`.
2. `.example vector --run` executed successfully in interactive shell.

## Sprint Gate Conclusion

1. SQL-native vector operator syntax is implemented and runnable through `sqlrite sql`.
2. Vector helper function baseline for SQL retrieval is in place.
3. Parser rewrite + regression tests are green with deterministic behavior on fixed input.
