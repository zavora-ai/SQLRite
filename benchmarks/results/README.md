# Benchmark Results

This directory contains benchmark result artifacts.

- `legacy/` holds historical benchmark JSON files that were previously stored at the repo root.
- `project_plan/reports/` remains the location for sprint and release evidence generated by benchmark/reliability workflows.

The runtime benchmark defaults still use `examples/eval_dataset.json` as the canonical sample dataset path.
